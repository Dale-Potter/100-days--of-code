#README

Data storage with GUI. This will be the fully working app with out the scheduler.

A text file will be used to store data collected. In a format that will be suitable for analysis with pandas and matplot lib later o.

NOTES:

1 - Lambda function used to retrieve data from slider (Event handler). A defined fucntion would run automatically when toplevel was opened.
2 - Possible improvement, could add a graph using matplot to show concentration levels against time. Either as another pop up or on the tracker page.
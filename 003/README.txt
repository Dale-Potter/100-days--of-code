#VisualCodeCounter

This script will take in the data stored from CodeCounter and convert into a dataframe.

Then present data in a useful visual format.

Using in anaconda Jupyter notebooks, help with visualisation tools